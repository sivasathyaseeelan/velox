import type { PublicKey } from "#/gen-proto/nillion/auth/v1/public_key_pb";
import { type UserId as UserIdProto } from "#/gen-proto/nillion/auth/v1/user_pb";
export declare class UserId {
    private inner;
    constructor(inner: Uint8Array);
    toHex(): string;
    toProto(): UserIdProto;
    static fromProto(id: UserIdProto): UserId;
    static from(key: PublicKey): UserId;
}
